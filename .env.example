# This is a fully static Vite + React site and currently needs no
# environment variables to build or run.
#
# Add any future client-side config here using the VITE_ prefix so it is
# exposed to the app, e.g.:
# VITE_GA_MEASUREMENT_ID="G-XXXXXXXXXX"
